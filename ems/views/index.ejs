<!DOCTYPE html>
<html lang="en">
    <%- include('header'); %>
    

<body>
    <%- include('navbar'); %>

    <!-- Main jumbotron for a primary marketing or call to action  -->
    <div class="jumbotron" style="background-color:#eef5e1;">
        <div class="container">
            <h1 class="display-3">Employee Management System</h1>
            <p>
                This is a template for a simple marketing or informational website. It
                includes a large callout called a jumbotron and three supporting
                pieces of content. Use it as a starting point to create something more
                unique.  
            </p>
            <p>
              <a class="btn btn-info btn-lg" href="#" role="button">Learn more &raquo;</a>  
            </p>
        </div>
    </div>

    <div class="container">
        <!-- Example row of columns -->
        <div class="row">
            <div class="col-md-8">
                <h2>Details about EMS</h2>
               
         
<div role="main" class="container" style="width: 30%;">
  <br /><br />

  <% if (employees.length) { %>
    <h2>Employees</h2>
    <br />
    <a class="" href="/new">New Employee</a>
    <br />
    <table class="table table-hover">
      <thead class="bg-secondary">
      <tr>
        <th class="text-white">Name</th>
        <th class="text-white">Functions</th>
      </tr>
      </thead>
      <tbody>
      <% employees.forEach(function (entry) { %>
        <tr>
          <td><%= entry.name %></td>
          <td style="width: 10%; text-align: center;">
            <a style="color: #555" href="/view/<%= entry.name %>">
              <i class="fa fa-pencil fa-fw" aria-hidden="true"></i>
            </a>
          </td>
        </tr>
      </tbody>
      <% }) %>
    </table>
  <% } else { %> No entries! <a href="/new">Add one!</a>
  <% } %>

  <br />
</div>
        
        <%- include('footer'); %>
    </div>

    <%- include('scripts'); %>
</body>
</html>